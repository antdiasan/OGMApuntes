# 3.3 Sintaxis y estructura básica de CSS

El código CSS se escribe siguiendo una **estructura simple**:

```css
selector {
  propiedad: valor;
}
```

- **Selector** → indica a qué elementos HTML se aplicará el estilo.  
- **Propiedad** → define la característica que se quiere modificar (color, tamaño, margen, etc.).  
- **Valor** → especifica el ajuste para esa propiedad.  

---

## Ejemplo

```css
p {
  color: blue;
  font-size: 16px;
}
```

👉 Este código aplicará color azul y tamaño de letra 16px a todos los párrafos `<p>`.

---

## Múltiples reglas

Un archivo CSS puede contener muchas reglas:

```css
h1 {
  color: darkred;
  text-align: center;
}

a {
  color: green;
  text-decoration: none;
}
```

---

## Agrupación de selectores

Podemos aplicar los mismos estilos a varios elementos separando con comas:

```css
h1, h2, h3 {
  font-family: Arial, sans-serif;
  color: navy;
}
```

---

## Comentarios en CSS

Los comentarios ayudan a organizar el código y se escriben así:

```css
/* Este es un comentario en CSS */
p {
  line-height: 1.5;
}
```

---

## Buenas prácticas

| ✔️ Hacer | ❌ Evitar |
|----------|-----------|
| Usar comentarios para organizar estilos. | Escribir CSS sin explicación en proyectos largos. |
| Agrupar selectores con propiedades comunes. | Repetir reglas idénticas para cada etiqueta. |
| Mantener el mismo estilo de indentación. | Mezclar distintas formas de sangría y espaciado. |

---

## 📌 Resumen

- La sintaxis de CSS sigue la forma: `selector { propiedad: valor; }`.  
- Los selectores indican qué elementos se verán afectados.  
- Se pueden agrupar selectores y añadir comentarios para mejorar la organización.  
