# 3.2 Formas de aplicar CSS

CSS se puede aplicar en un documento HTML de **tres maneras principales**:  
1. En línea (inline).  
2. Interna (dentro de `<style>`). 
3. Externa (archivo `.css`).  

---

## 1. CSS en línea (inline)

Se aplica directamente sobre el atributo `style` de una etiqueta HTML.

```html
<p style="color: red; font-size: 20px;">Este texto es rojo y más grande.</p>
```

⚠️ **No recomendado** para proyectos grandes, porque mezcla HTML y CSS, dificultando el mantenimiento.

---

## 2. CSS interno

Se incluye dentro de la sección `<head>` del documento HTML, usando la etiqueta `<style>`.

```html
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Ejemplo CSS interno</title>
  <style>
    p {
      color: blue;
      font-size: 18px;
    }
  </style>
</head>
<body>
  <p>Este texto es azul.</p>
</body>
</html>
```

👉 Útil en documentos pequeños o pruebas rápidas.

---

## 3. CSS externo

Este se puede aplicar de dos formas distintas: 


### 3.1. Importando

Se guarda en un archivo separado (ej. `estilos.css`) y se importa utilizando la **directiva @import**:

```html
<style>@import 'css/import.css'; </style>
```

Utilizar esta técnica me permite realizar cambios en el estilo de toda mi web únicamente tocando un fichero.

Sin embargo hay un inconveniente: **el rendimiento**, ya que es bloqueante en algunos navegadores, y se para la descarga del resto de la página hasta que no se ha descargado la hoja de estilos.

👉 Por lo tanto hay que usar esta opción con cuidado.

---

## 3.2. Vinculado (link)

Se guarda en un archivo separado (ej. `estilos.css`) y se enlaza desde el HTML con `<link>`.

```html
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Ejemplo CSS externo</title>
  <link href="css/estilos.css" rel="stylesheet" />
</head>
<body>
  <h1>Hola mundo con CSS externo</h1>
</body>
</html>
```

Tiene dos atributos en los que indicamos:

- **href**: qué fichero quiero, 
- **rel**: relación que existe entre el documento y el documento enlazado.

**Archivo estilos.css**
```css
h1 {
  color: green;
  text-align: center;
}
```

✅ **Opción recomendada** para proyectos reales, porque separa contenido y presentación, y facilita reutilizar estilos en varias páginas.

---

## Comparativa de las formas de aplicar CSS

| Forma   | Ventajas | Inconvenientes |
|---------|----------|----------------|
| En línea | Rápido para pruebas puntuales. | Código desordenado, difícil de mantener. |
| Interna | Útil en páginas pequeñas. | No reutilizable en otras páginas. |
| Importado | Reutilizable, ordenado y limpio. | Baja el rendimiento, respecto al LINK. |
| Vinculado | Reutilizable, ordenado y limpio. | Requiere cargar un archivo extra. |

---

## 📌 Resumen

- CSS se puede aplicar en línea, de forma interna o externa.  
- La forma **más profesional** y recomendada es el uso de **hojas de estilo externas**.  
- Separar estructura (HTML) y presentación (CSS) mejora el mantenimiento y la organización del proyecto.  
