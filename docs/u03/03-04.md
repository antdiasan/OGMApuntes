# 3.4 Selectores en CSS

Los **selectores** son la parte de una regla CSS que indican **a qué elementos HTML** se aplicarán los estilos.  
Gracias a ellos podemos ser muy específicos o generales según nuestras necesidades.

---

## Selectores básicos

- **Por etiqueta (tipo)**: selecciona todos los elementos de un tipo.

```css
p {
  color: blue;
}
```
Aplica color azul a todos los párrafos `<p>`.

- **Por clase**: selecciona los elementos con una clase concreta. Se usa `.`

```css
.destacado {
  background-color: yellow;
}
```

donde el código html sería:
```
<p class="destacado">Este párrafo tiene una clase llamada "destacado".</p>
<p>Este párrafo no tiene clase.</p>
```

Aplica fondo amarillo a todos los elementos con `class="destacado"`.

- **Por id**: selecciona un elemento único con un identificador. Se usa `#`

```css
#principal {
  font-size: 20px;
}
```

donde el código html sería:
```
<h1 id="principal">Título principal de la página</h1>
```

Aplica tamaño de fuente 20px al elemento con `id="principal"`.

---

## Selectores combinados

- **Agrupados**: aplican el mismo estilo a varios selectores.

```css
h1, h2, h3 {
  font-family: Arial, sans-serif;
}
```

- **Descendientes**: selecciona elementos dentro de otro.

```css
div p {
  color: gray;
}
```

Cambia el color de todos los `<p>` que estén dentro de un `<div>`.

- **Hijos directos**: solo selecciona los hijos inmediatos.

```css
div > p {
  color: red;
}
```

Ejemplo de descendiente y de hijo directo en html:
```
<div>
  <p>Este párrafo es hijo directo de div.</p>
  <section>
    <p>Este párrafo es descendiente pero NO hijo directo.</p>
  </section>
</div>
```

---

## Selectores de atributo

Podemos seleccionar elementos según sus atributos:

```css
input[type="text"] {
  border: 1px solid black;
}
```

Este selector aplica borde solo a los inputs de tipo texto.

---

## Selector universal

El asterisco `*` selecciona **todos los elementos**.

```css
* {
  margin: 0;
  padding: 0;
}
```

Útil para hacer un "reset" inicial de estilos.

---

## Buenas prácticas

| ✔️ Hacer | ❌ Evitar |
|----------|-----------|
| Usar clases para aplicar estilos reutilizables. | Abusar de `id` para dar estilo (mejor para JS). |
| Agrupar selectores con estilos comunes. | Repetir reglas idénticas para cada etiqueta. |
| Usar nombres de clases descriptivos. | Nombres genéricos como `.azul` o `.grande`. |

---

## 📌 Resumen

- Los selectores definen **qué elementos serán afectados** por una regla CSS.  
- Tipos principales: por etiqueta, clase e id.  
- Se pueden combinar selectores para mayor precisión.  
- Los selectores de atributo permiten aplicar estilos según valores específicos.  
