# 3.7 Resumen y ejercicios de la unidad

En esta unidad hemos visto los **fundamentos de CSS**, que nos permiten separar el contenido (HTML) de la presentación (diseño).  
Antes de pasar a propiedades más avanzadas, repasemos lo aprendido y pongámoslo en práctica.

---

## 📖 Resumen de conceptos clave

- **Qué es CSS**: lenguaje de estilos que controla la apariencia de los documentos HTML.  
- **Formas de aplicar CSS**: en línea (`style`), interna (`<style>` en el `<head>`) y externa (archivo `.css`).  
- **Sintaxis básica**:  
  ```css
  selector {
    propiedad: valor;
  }
  ```
- **Selectores básicos**: por etiqueta (`p`), clase (`.destacado`), id (`#principal`).  
- **Selectores avanzados**: agrupados, descendientes, hijos directos, de atributo, pseudoclases y pseudoelementos.  
- **Cascada**: los estilos se aplican según origen, especificidad y orden en el código.  
- **Herencia**: ciertas propiedades (como `color`, `font-family`) se transmiten de padres a hijos.  
- **Especificidad**: determina qué regla gana: etiqueta < clase < id < inline.  

---

## 📝 Ejercicios prácticos

### Ejercicio 1: Hojas externas
Crea un archivo `estilos.css` y enlázalo desde un HTML. Aplica un color de fondo distinto al `<body>` y cambia el color de los títulos `<h1>`.

### Ejercicio 2: Clases e IDs
En un HTML con varios párrafos:  
- Aplica una clase llamada `.importante` a algunos de ellos para poner el texto en negrita.  
- Aplica un id `#intro` al primero y cámbiale el color.  

### Ejercicio 3: Herencia
Define en el `<body>` un `font-family` y observa cómo se aplica automáticamente a los hijos `<p>` y `<h2>`.  
Prueba con una propiedad que **no se herede** (ej. `border`) y observa la diferencia.

### Ejercicio 4: Especificidad
Crea un párrafo con `id="especial"` y clase `"destacado"`. Define las siguientes reglas:

```css
p { color: blue; }
.destacado { color: red; }
#especial { color: green; }
```

👉 Explica de qué color se verá el texto y por qué.

---

## 💡 Retos extra

1. Diseña una **tarjeta de presentación** con HTML y CSS básico. Debe incluir un nombre, un cargo y un fondo de color.  
2. Experimenta con selectores **descendientes** y **hijos directos** para ver la diferencia práctica en un mismo HTML.  

---

## 📌 Conclusión de la unidad

- CSS es la herramienta fundamental para dar **estilo y diseño** a una página web.  
- Separar estructura (HTML) y presentación (CSS) hace los proyectos más limpios y fáciles de mantener.  
- Comprender la **cascada, herencia y especificidad** es clave para resolver conflictos de estilos.  

👉 Con esta base, estamos listos para pasar a propiedades avanzadas y maquetación en la siguiente unidad.
