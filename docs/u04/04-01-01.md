# 4.1.1 Colores y fondos

En CSS, los **colores y fondos** permiten dar estilo y personalidad a los elementos de una página web.

---

## 🎨 Colores en CSS

Existen varias formas de definir colores:

### 1. Palabras clave
```css
p { color: red; }
h1 { color: blue; }
```
CSS reconoce un conjunto de nombres predefinidos como `red`, `blue`, `green`, `black`, `white`, etc.

### 2. Valores hexadecimales
```css
p { color: #ff0000; }  /* Rojo */
```
Se usan 6 dígitos hexadecimales (0-9 y A-F), con dos por cada canal RGB (rojo, verde, azul).

### 3. Función RGB / RGBA
```css
h1 { color: rgb(0, 128, 0); }        /* Verde */
h2 { color: rgba(0, 128, 0, 0.5); }  /* Verde semitransparente */
```

### 4. Función HSL / HSLA
```css
h1 { color: hsl(0, 100%, 50%); }       /* Rojo */
h2 { color: hsla(0, 100%, 50%, 0.5); } /* Rojo semitransparente */
```
- **H**: tono (0-360).  
- **S**: saturación (%).  
- **L**: luminosidad (%).  

---

## 🖼️ Fondos en CSS

### 1. Color de fondo
```css
body { background-color: lightgray; }
```

### 2. Imagen de fondo
```css
body {
  background-image: url("fondo.jpg");
}
```

### 3. Repetición del fondo
```css
body {
  background-image: url("fondo.png");
  background-repeat: no-repeat;
}
```

Valores:  
- `repeat` (por defecto).  
- `repeat-x` (horizontal).  
- `repeat-y` (vertical).  
- `no-repeat` (sin repetición).  

### 4. Posición del fondo
```css
body {
  background-image: url("fondo.png");
  background-repeat: no-repeat;
  background-position: center top;
}
```

### 5. Tamaño del fondo
```css
body {
  background-image: url("fondo.jpg");
  background-size: cover;
}
```

Valores:  
- `auto`: tamaño original.  
- `cover`: ajusta para cubrir todo el área.  
- `contain`: ajusta para que la imagen quepa completa.  

### 6. Fondo fijo o desplazable
```css
body {
  background-image: url("fondo.jpg");
  background-attachment: fixed;
}
```

### 7. Propiedad abreviada
Podemos combinar todo en una sola regla:

```css
body {
  background: url("fondo.jpg") no-repeat center center fixed;
  background-size: cover;
}
```

---

## ✅ Buenas prácticas

- Usar valores **hexadecimales o HSL** para mayor control de los colores.  
- Asegurar siempre **contraste suficiente** entre texto y fondo.  
- Evitar fondos con demasiado detalle que dificulten la lectura.  
- Optimizar las imágenes para reducir tiempos de carga.  

---

## 📌 Resumen

- Los colores se pueden definir con nombres, hexadecimales, RGB(A) y HSL(A).  
- Los fondos permiten aplicar color sólido, imágenes, posiciones, tamaños y repeticiones.  
- Es recomendable mantener **claridad y contraste** para mejorar la legibilidad.  
