# 4.1.4 Unidades y medidas en CSS

Las unidades en CSS determinan cómo se representan los tamaños, longitudes y proporciones en una página web. Es fundamental conocerlas para lograr diseños adaptables y consistentes.

---

## 📏 Unidades absolutas

Las unidades absolutas tienen un valor fijo que no depende de otros elementos ni del tamaño de la pantalla.

- `px` → píxeles. La más usada en diseño web, aunque no es adaptable por sí sola.
- `cm`, `mm`, `in` → centímetros, milímetros y pulgadas. Apenas se usan en web, más en impresión.
- `pt`, `pc` → puntos y picas. Más útiles en tipografía impresa que en pantalla.

Ejemplo:

```css
h1 { font-size: 24px; }
p  { margin: 10mm; }
```

---

## 📐 Unidades relativas

Se calculan en relación a otro valor, lo que permite un diseño más flexible y adaptativo.

- `em` → relativo al tamaño de fuente del elemento padre.
- `rem` → relativo al tamaño de fuente del elemento raíz (`html`).
- `%` → relativo al tamaño del contenedor.
- `vw` → relativo al ancho total de la ventana (1vw = 1% del viewport width).
- `vh` → relativo a la altura de la ventana (1vh = 1% del viewport height).
- `vmin` → relativo al lado más pequeño de la ventana.
- `vmax` → relativo al lado más grande de la ventana.

Ejemplo:

```css
h1 { font-size: 2em; }    /* doble del tamaño de la fuente base */
p  { font-size: 1rem; }   /* igual al tamaño raíz */
div { width: 50%; }       /* ocupa la mitad del contenedor */
section { height: 80vh; } /* 80% de la altura de la ventana */
```

---

## ✅ Buenas prácticas

- Usar `px` solo cuando se requieran medidas exactas.
- Preferir `em` y `rem` para tamaños de texto → mejora la accesibilidad.
- Usar `vw` y `vh` para layouts responsivos.
- Evitar mezclar demasiadas unidades en un mismo diseño.
- Mantener coherencia en el uso de unidades en todo el proyecto.

---

## 📌 Resumen

- **Unidades absolutas** → valores fijos, poco flexibles en diseño web.  
- **Unidades relativas** → adaptativas, ideales para diseño responsivo.  
- Conocer y aplicar correctamente estas unidades permite crear páginas más **accesibles, coherentes y adaptables**.  
