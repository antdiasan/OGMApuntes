# 4.1.3 Bordes, márgenes y padding

En CSS, los elementos se representan como cajas rectangulares. Los bordes, márgenes y el padding permiten controlar el espacio y la separación entre estas cajas y su contenido.

---

## 🔲 Bordes (`border`)

El borde es la línea que rodea al contenido y al padding de un elemento.

### Propiedades principales:
- `border-width` → grosor del borde.
- `border-style` → estilo (solid, dashed, dotted, double, none, etc.).
- `border-color` → color del borde.

Se pueden combinar en una sola propiedad abreviada:

```css
div {
  border: 2px solid blue;
}
```

### Bordes redondeados
Con `border-radius` se pueden crear esquinas redondeadas.

```css
div {
  border: 2px solid red;
  border-radius: 10px;
}
```

---

## 📏 Márgenes (`margin`)

El margen es el espacio externo entre el borde del elemento y los demás elementos.

- Propiedades: `margin-top`, `margin-right`, `margin-bottom`, `margin-left`.
- Forma abreviada: `margin: 10px 20px;`  
  - 10px arriba y abajo, 20px izquierda y derecha.

Ejemplo:

```css
p {
  margin: 20px;
}
```

### Colapso de márgenes
Cuando dos márgenes verticales se tocan, se combinan y se aplica el mayor valor en lugar de sumarse.

---

## 🧩 Padding

El padding es el espacio interno entre el contenido y el borde.

- Propiedades: `padding-top`, `padding-right`, `padding-bottom`, `padding-left`.
- Forma abreviada: `padding: 15px;`

Ejemplo:

```css
div {
  padding: 15px;
}
```

---

## ⚖️ Diferencias clave

| Propiedad | Qué controla | Ejemplo |
|-----------|--------------|---------|
| `margin`  | Espacio externo entre la caja y otras | Distancia entre dos bloques |
| `padding` | Espacio interno entre contenido y borde | Relleno dentro del div |
| `border`  | Línea visible que rodea el contenido y padding | Marco azul o rojo |

---

## ✅ Buenas prácticas

- Usar unidades relativas (`em`, `%`) para diseños flexibles.  
- Controlar márgenes y paddings para evitar espacios excesivos.  
- Usar `margin: auto;` para centrar elementos horizontalmente.  
- No abusar de bordes gruesos, ya que pueden romper la estética.  

---

## 📌 Resumen

- `border` → define límites visibles de la caja.  
- `margin` → controla el espacio externo.  
- `padding` → controla el espacio interno.  
- Su correcta combinación es fundamental para un diseño limpio y ordenado en CSS.  
