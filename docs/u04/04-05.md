# 4.5 Maquetación con Grid

**CSS Grid** es un sistema de maquetación **bidimensional** (filas y columnas) que permite crear diseños complejos con poco código. Complementa a Flexbox: usa **Grid para estructuras 2D** y **Flex** para alinear contenido dentro de cada área.

---

## 🚀 Activar Grid

```css
.contenedor {
  display: grid;
}
```

---

## 🧱 Definir columnas y filas

```css
.contenedor {
  display: grid;
  grid-template-columns: 200px 1fr 2fr; /* 3 columnas */
  grid-template-rows: auto 300px;       /* 2 filas */
  gap: 1rem;                             /* separaciones */
}
```

- `fr` reparte **fracciones** del espacio disponible.  
- `auto` se ajusta al contenido.  
- `gap`, `row-gap`, `column-gap` para separar celdas.

### Repeticiones y tamaños flexibles
```css
.contenedor {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  grid-template-rows: repeat(2, minmax(120px, auto));
}
```

- `repeat(n, …)` simplifica patrones.  
- `minmax(min, max)` fija límites flexibles.

---

## 🧲 Colocar ítems en la rejilla

```css
.item-a { grid-column: 1 / 3; grid-row: 1 / 2; } /* ocupa 2 columnas */
.item-b { grid-column: 3 / 4; grid-row: 1 / 3; } /* ocupa 2 filas */
```

Atajos:
```css
.item-a { grid-area: 1 / 1 / 2 / 3; } /* row-start / col-start / row-end / col-end */
```

---

## 🗺️ Áreas con nombre

```css
.layout {
  display: grid;
  grid-template-areas:
    "header header"
    "sidebar main"
    "footer footer";
  grid-template-columns: 240px 1fr;
  grid-template-rows: auto 1fr auto;
  gap: 1rem;
}

.header  { grid-area: header; }
.sidebar { grid-area: sidebar; }
.main    { grid-area: main; }
.footer  { grid-area: footer; }
```

---

## 🎯 Alineación en Grid

- En celdas: `justify-items` (horizontal), `align-items` (vertical) y `place-items` (atajo).  
- En el **conjunto** de filas/columnas: `justify-content`, `align-content`, `place-content`.

```css
.contenedor { place-items: center; }        /* centra cada ítem */
.contenedor { place-content: center; }      /* centra el conjunto */
.item       { place-self: start end; }      /* un ítem concreto */
```

---

## ⚙️ Rejilla implícita y flujo

Cuando se añaden más ítems de los definidos por plantilla:
```css
.contenedor {
  display: grid;
  grid-auto-rows: 200px;         /* altura para filas implícitas */
  grid-auto-flow: row dense;     /* flujo y recolocación (dense) */
}
```

- `grid-auto-flow: row|column|dense` controla cómo se colocan los ítems no posicionados.

---

## 📱 Patrón responsive de tarjetas (auto-fit / auto-fill)

```css
.cards {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 1rem;
}
```
- `auto-fit` **comprime** las columnas vacías; `auto-fill` **reserva** huecos.  
- `minmax(220px, 1fr)` asegura un ancho mínimo y que el resto sea flexible.

---

## 🔄 Grid vs Flex: ¿cuándo usar cada uno?

- **Grid** → estructura **2D** (cabecera, sidebar, main, footer; galerías).  
- **Flex** → **alineación 1D** (barras, menús, grupos de botones).  
- Se pueden **combinar**: Grid para el layout global y Flex dentro de cada área.

---

## ✅ Buenas prácticas

- Empezar por una plantilla clara: columnas, filas y `gap` coherentes.  
- Usar **áreas con nombre** para semántica y mantenimiento.  
- Preferir `fr`, `minmax`, `auto-fit/auto-fill` para layouts fluidos.  
- No forzar posiciones con muchos índices si puedes agrupar por áreas.  
- Probar el layout con **herramientas de desarrollo** (inspector Grid).

---

## 📌 Resumen

- Grid organiza **filas y columnas** con precisión.  
- Permite colocar ítems por índices o por **áreas con nombre**.  
- Con `fr`, `minmax` y `auto-fit/auto-fill` se logran rejillas **responsivas** con poco código.  
- Úsalo junto a **Flex** para componentes internos.
