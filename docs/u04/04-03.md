# 4.3 Posicionamiento y display

En esta lección veremos cómo controlar **cómo se muestran** los elementos (propiedad `display`) y **dónde se colocan** dentro de la página (propiedad `position`).

---

## 🧱 Propiedad `display` (modo de visualización)

### 1) `block`
El elemento **ocupa todo el ancho disponible** y empieza en una línea nueva. Se pueden definir `width/height`, `margin` y `padding`.
```css
div, p, h1 { display: block; }
```

### 2) `inline`
El elemento **no inicia línea nueva** y solo ocupa el espacio de su contenido. **No** respetan `width/height` (sí márgenes horizontales).
```css
span, a, strong { display: inline; }
```

### 3) `inline-block`
Como `inline`, **pero** permite `width/height`, `margin` y `padding` completos. Útil para menús horizontales, tarjetas, etc.
```css
.menu-item { display: inline-block; }
```

### 4) `none`
Oculta el elemento y **no ocupa espacio** en el flujo del documento.
```css
.oculto { display: none; }
```

> Nota: Además de estos, existen `flex` y `grid`.

---

## 📍 Propiedad `position` (colocación)

Controla **cómo** y **respecto a qué** se posiciona un elemento.

### 1) `static` (por defecto)
Sigue el flujo normal (no admite `top/right/bottom/left`).

### 2) `relative`
Se **desplaza** respecto a su posición original **sin sacarlo** del flujo.
```css
.caja-rel {
  position: relative;
  top: 10px;   /* baja 10px desde su posición normal */
  left: 5px;   /* se mueve 5px a la derecha */
}
```

### 3) `absolute`
Se posiciona **respecto al antecesor posicionado** más cercano (`position` distinto de `static`). Sale del flujo normal.
```css
.contenedor {
  position: relative; /* referencia del hijo absoluto */
}
.hijo-abs {
  position: absolute;
  top: 10px;
  right: 10px;
}
```

### 4) `fixed`
Se fija **respecto a la ventana** (viewport). Permanece visible aunque haya scroll.
```css
.boton-flotante {
  position: fixed;
  bottom: 20px;
  right: 20px;
}
```

### 5) `sticky`
Se comporta como `relative` hasta alcanzar un umbral; entonces se queda “pegado” como `fixed`.
```css
.cabecera {
  position: sticky;
  top: 0; /* umbral de pegado */
  background: white;
}
```

---

## 🧩 `z-index` (superposición)

Controla el **orden de apilamiento** cuando los elementos se solapan. Solo funciona en elementos **posicionados** (no `static`).
```css
.capa-1 { position: relative; z-index: 1; }
.capa-2 { position: relative; z-index: 10; } /* queda por encima */
```

> Cuidado con los **contextos de apilamiento** (se crean con `position` + `z-index`, `opacity < 1`, `transform`, etc.).

---

## 🌊 `float` y `clear` (histórico)

`float` se usaba para maquetar columnas, hoy se prefiere **Flex** y **Grid**. Aun así, conviene conocerlo:
```css
img.izq { float: left; margin: 0 1rem 1rem 0; }
.parrafo { clear: both; }
```

---

## 🧪 Ejemplos rápidos

```html
<div class="hero">
  <button class="cta">Empezar</button>
</div>
```
```css
.hero { position: relative; height: 240px; background: #eef; }
.cta  {
  position: absolute; bottom: 16px; right: 16px;
  display: inline-block; padding: .5rem 1rem;
}
```

```html
<header class="sticky-header">Menú</header>
<main>...contenido...</main>
```
```css
.sticky-header {
  position: sticky; top: 0; background: #fff;
  border-bottom: 1px solid #ddd;
}
```

---

## ✅ Buenas prácticas

- Usar `inline-block` para elementos en línea con dimensiones (o pasar a **flex/grid**).
- Evitar abusar de `absolute`: dificulta el responsive.
- `sticky` es ideal para cabeceras y barras de navegación.
- Gestionar `z-index` con una **escala acotada** (ej.: 1, 10, 100…) para evitar guerras de capas.
- Para maquetación en serio, **Flex** (4.4) y **Grid** (4.5) son la opción moderna.

---

## 📌 Resumen

- `display` define el **modo de caja** (flujo y dimensiones).  
- `position` define **cómo y respecto a qué** se coloca un elemento.  
- `z-index` controla la **superposición**.  
- `float/clear` son heredados del pasado; hoy preferimos Flex y Grid.
