# 4.2 El Modelo de Caja (Box Model)

El **Modelo de Caja** es uno de los conceptos más importantes en CSS. Todos los elementos de una página web se representan como cajas rectangulares que se componen de varias áreas: **contenido, padding, borde y margen**.

---

## 📦 Partes del modelo de caja

1. **Contenido (`content`)**
      - Área donde se muestra el texto, imágenes u otros elementos.  
      - Su tamaño se controla con propiedades como `width` y `height`.
2. **Padding (`padding`)**   
      - Espacio entre el contenido y el borde.  
      - Aumenta el área de la caja sin afectar al contenido.  
3. **Borde (`border`)** 
      - Línea que rodea el contenido y el padding.  
      - Puede tener estilo, color y grosor.
4. **Margen (`margin`)**
      - Espacio externo entre la caja y los elementos adyacentes.  

---

## 📐 Representación gráfica

```
+---------------------------+
|        margin             |
|  +---------------------+  |
|  |       border        |  |
|  |  +---------------+  |  |
|  |  |    padding    |  |  |
|  |  |  +---------+  |  |  |
|  |  |  | content |  |  |  |
|  |  |  +---------+  |  |  |
|  |  +---------------+  |  |
|  +---------------------+  |
+---------------------------+
```

---

## 🎯 Ejemplo práctico

```css
div.caja {
  width: 200px;
  height: 100px;
  padding: 20px;
  border: 5px solid blue;
  margin: 15px;
}
```

En este caso:

- `content`: 200px × 100px  
- `padding`: 20px a cada lado → añade espacio interno  
- `border`: 5px a cada lado → añade grosor alrededor  
- `margin`: 15px alrededor de la caja  

Tamaño total = **contenido + padding + border + margin**

---

## ⚙️ Propiedad `box-sizing`

Por defecto, CSS calcula el ancho y alto de un elemento **solo con el contenido** (`content-box`).  
Para simplificar, se puede usar:

- `box-sizing: content-box;` (valor por defecto).  
- `box-sizing: border-box;` (incluye padding y border dentro del ancho/alto).  

Ejemplo:

```css
.caja1 {
  width: 200px;
  padding: 20px;
  box-sizing: content-box; /* Total = 240px */
}

.caja2 {
  width: 200px;
  padding: 20px;
  box-sizing: border-box;  /* Total = 200px */
}
```

---

## ✅ Buenas prácticas

- Usar `box-sizing: border-box;` en todo el proyecto (más predecible).  
- Ser coherente con el uso de `margin` y `padding` para mantener un diseño limpio.  
- Probar siempre los cambios con herramientas de inspección del navegador.  

---

## 📌 Resumen

- El modelo de caja define cómo se calcula el espacio de un elemento en la página.  
- Incluye: **contenido, padding, borde y margen**.  
- `box-sizing: border-box;` facilita el control del tamaño.  
