# 4.8 Animaciones con keyframes en CSS

Además de las transiciones, CSS permite crear **animaciones complejas y personalizadas** mediante la regla `@keyframes`.  
Con ella podemos definir **etapas intermedias** de una animación y aplicarlas a un elemento.

---

## 🎬 Sintaxis básica de @keyframes

```css
@keyframes nombre_animacion {
  from { propiedad: valor_inicial; }
  to   { propiedad: valor_final; }
}
```

o bien usando porcentajes:

```css
@keyframes mover {
  0%   { transform: translateX(0); }
  50%  { transform: translateX(100px); }
  100% { transform: translateX(0); }
}
```

---

## 🔧 Aplicación de la animación

Se hace mediante la propiedad `animation`:

```css
selector {
  animation-name: mover;
  animation-duration: 2s;
  animation-timing-function: ease-in-out;
  animation-delay: 0s;
  animation-iteration-count: infinite;
  animation-direction: alternate;
}
```

### Propiedades clave
- `animation-name`: nombre definido en `@keyframes`.  
- `animation-duration`: tiempo de la animación (ej: `2s`).  
- `animation-timing-function`: curva de velocidad (`linear`, `ease`, `ease-in`, `cubic-bezier()`).  
- `animation-delay`: retraso antes de iniciar.  
- `animation-iteration-count`: número de repeticiones (`1`, `infinite`).  
- `animation-direction`: `normal`, `reverse`, `alternate`.  
- `animation-fill-mode`: cómo se comporta antes/después (`none`, `forwards`, `backwards`, `both`).  

Ejemplo con atajo:
```css
div {
  animation: mover 2s infinite alternate ease-in-out;
}
```

---

## 🧩 Ejemplo práctico: botón parpadeante

```css
@keyframes parpadeo {
  0%, 100% { opacity: 1; }
  50%      { opacity: 0; }
}

button {
  background: #e74c3c;
  color: white;
  padding: 10px 20px;
  border: none;
  animation: parpadeo 1s infinite;
}
```

El botón cambia de opacidad continuamente.

---

## ✅ Buenas prácticas
- Usar animaciones para **resaltar interacciones** (ej: notificaciones, hover), no de forma excesiva.  
- Evitar animar propiedades que afecten al **layout** (`width`, `height`) → usar `transform` u `opacity`.  
- Controlar la **duración y repetición** para que no resulten molestas.  
- Combinarlas con `transition` para efectos más naturales.  

---

## 📌 Resumen
- `@keyframes` define **etapas intermedias** en una animación.  
- Las propiedades `animation-*` controlan duración, repeticiones y dirección.  
- Permiten crear interfaces **interactivas y modernas** con solo CSS.
