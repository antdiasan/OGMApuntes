# 4.7 Transiciones y transformaciones en CSS

CSS permite crear efectos dinámicos y animaciones sencillas mediante **transiciones** y **transformaciones**, lo que mejora la experiencia de usuario sin necesidad de JavaScript.

---

## 🎨 Transiciones

Las **transiciones** permiten que los cambios de una propiedad CSS ocurran de forma **suave** en lugar de instantánea.

### Sintaxis básica
```css
selector {
  transition: propiedad duración función-retardo;
}
```

### Ejemplo simple
```css
button {
  background-color: blue;
  transition: background-color 0.3s ease;
}

button:hover {
  background-color: red;
}
```
Al pasar el ratón, el color de fondo cambia gradualmente de azul a rojo.

### Propiedades clave
- `transition-property`: la propiedad que se anima (`color`, `background`, `transform`, etc.).  
- `transition-duration`: cuánto dura la transición (`0.5s`, `200ms`).  
- `transition-timing-function`: curva de velocidad (`ease`, `linear`, `ease-in`, `ease-out`, `cubic-bezier()`).  
- `transition-delay`: tiempo de espera antes de iniciar.  

Ejemplo con varias propiedades:
```css
div {
  transition: all 0.5s ease-in-out;
}
```

---

## 🔄 Transformaciones

Las **transformaciones** permiten modificar la forma o posición de un elemento sin alterar el flujo del documento.

### Funciones comunes de `transform`
- `translate(x,y)`: mover.  
- `scale(x,y)`: escalar.  
- `rotate(ángulo)`: rotar.  
- `skew(x,y)`: inclinar.  

Ejemplo:
```css
div {
  transform: translateX(50px) rotate(45deg);
}
```

---

## 🧩 Ejemplo práctico: botón animado

```css
button {
  padding: 10px 20px;
  background: #3498db;
  color: white;
  border: none;
  transition: transform 0.3s ease, background 0.3s ease;
}

button:hover {
  background: #2ecc71;
  transform: scale(1.1);
}
```

---

## ✅ Buenas prácticas
- Usar transiciones para **mejorar usabilidad**, no como decoración excesiva.  
- Aplicar `transform` en lugar de cambiar `top/left` → más eficiente en rendimiento.  
- Evitar animar propiedades pesadas (`box-shadow`, `width`) en exceso.  
- Combinar con `:hover`, `:focus` y `:active` para mejorar la interacción.  

---

## 📌 Resumen
- **Transiciones** → cambios suaves entre estados.  
- **Transformaciones** → mover, rotar, escalar o inclinar elementos.  
- Combinadas, permiten crear **interfaces modernas y fluidas** sin necesidad de JavaScript.
