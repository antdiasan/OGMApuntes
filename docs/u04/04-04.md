# 4.4 Maquetación con Flexbox

El modelo **Flexbox** (Flexible Box Layout) es una técnica de CSS que facilita la **distribución de elementos en una dimensión** (horizontal o vertical), permitiendo crear diseños flexibles y responsivos con poco código.

---

## 📦 Contenedor flexible

Para activar Flexbox se aplica `display: flex;` al contenedor.

```css
.container {
  display: flex;
}
```

### Propiedades principales del contenedor

- **flex-direction**: define la dirección de los ítems.
    - `row` (por defecto): horizontal de izquierda a derecha.
    - `row-reverse`: horizontal de derecha a izquierda.
    - `column`: vertical de arriba a abajo.
    - `column-reverse`: vertical de abajo a arriba.

- **flex-wrap**: controla si los ítems se ajustan a varias líneas.
    - `nowrap` (por defecto).
    - `wrap`: permite que salten de línea.
    - `wrap-reverse`: salto de línea en orden inverso.

- **justify-content**: alinea los ítems en el eje principal.
    - `flex-start`, `flex-end`, `center`, `space-between`, `space-around`, `space-evenly`.

- **align-items**: alinea los ítems en el eje transversal.
    - `stretch` (por defecto), `flex-start`, `flex-end`, `center`, `baseline`.

- **align-content**: alinea varias líneas dentro del contenedor.
    - Se aplica solo si hay más de una línea (`flex-wrap: wrap`).

Ejemplo:
```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  justify-content: space-between;
  align-items: center;
}
```

---

## 🧩 Ítems flexibles

Cada elemento hijo del contenedor flexible puede personalizarse con:

- **flex-grow**: factor de crecimiento (espacio extra).
- **flex-shrink**: factor de reducción (cuando no cabe).
- **flex-basis**: tamaño base del ítem antes de aplicar crecimiento/reducción.
- **flex**: abreviatura → `flex: grow shrink basis;`.

Ejemplo:
```css
.item {
  flex-grow: 1;   /* ocupa espacio extra disponible */
  flex-shrink: 0; /* no se encoge */
  flex-basis: 200px; /* tamaño base */
}
```

- **order**: cambia el orden de visualización de los ítems.
- **align-self**: sobrescribe la alineación del ítem individual.

```css
.item:first-child {
  order: 2;
  align-self: flex-end;
}
```

---

## 🎯 Ejemplos prácticos

### Barra de navegación con Flexbox
```html
<nav class="nav">
  <a href="#">Inicio</a>
  <a href="#">Servicios</a>
  <a href="#">Contacto</a>
</nav>
```

```css
.nav {
  display: flex;
  justify-content: space-around;
  background-color: #333;
}
.nav a {
  color: white;
  padding: 10px;
  text-decoration: none;
}
```

### Distribución de tarjetas responsivas
```html
<div class="cards">
  <div class="card">Card 1</div>
  <div class="card">Card 2</div>
  <div class="card">Card 3</div>
</div>
```

```css
.cards {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
}
.card {
  flex: 1 1 200px; /* crece, se reduce, base 200px */
  padding: 20px;
  background: lightblue;
}
```

---

## ✅ Buenas prácticas

- Usar Flexbox para **diseños unidimensionales** (filas o columnas).  
- Para diseños en dos dimensiones (filas y columnas) → usar **CSS Grid**.  
- Aprovechar propiedades abreviadas (`flex`, `gap`) para simplificar.  
- No abusar de `order`, mantener el orden semántico en HTML.  

---

## 📌 Resumen

- Flexbox organiza elementos en un eje principal y uno transversal.  
- Propiedades del contenedor: `flex-direction`, `justify-content`, `align-items`, etc.  
- Propiedades de los ítems: `flex-grow`, `flex-shrink`, `flex-basis`, `order`.  
- Ideal para barras de navegación, galerías y layouts adaptables.
