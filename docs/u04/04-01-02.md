# 4.1.2 Tipografía y texto

El control de la tipografía es fundamental en CSS, ya que influye en la **legibilidad**, la **jerarquía visual** y la **estética** de una página web.

---

## ✍️ Propiedades de fuente

### 1. `font-family`
Define la familia tipográfica. Es habitual indicar varias, en orden de preferencia, y terminar con una genérica:

```css
p {
  font-family: "Arial", "Helvetica", sans-serif;
}
```

### 2. `font-size`
Controla el tamaño de la fuente:

```css
h1 { font-size: 2em; }
p  { font-size: 16px; }
```

Unidades recomendadas: `em`, `rem`, `%` para adaptabilidad.

### 3. `font-weight`
Define el grosor del texto:

```css
p { font-weight: bold; }
h1 { font-weight: 700; }
```

### 4. `font-style`
Permite usar cursivas o texto normal:

```css
em { font-style: italic; }
```

### 5. `font-variant`
Convierte el texto en versalitas (small-caps):

```css
p { font-variant: small-caps; }
```

### 6. Propiedad abreviada `font`
Permite declarar varias propiedades en una sola línea:

```css
p {
  font: italic bold 16px/1.5 "Times New Roman", serif;
}
```

---

## 📝 Propiedades de texto

### 1. `color`
Color del texto:

```css
p { color: darkblue; }
```

### 2. `text-align`
Alineación del texto:

```css
h1 { text-align: center; }
p  { text-align: justify; }
```

### 3. `text-decoration`
Decoraciones como subrayado, tachado o sobrelineado:

```css
a { text-decoration: none; }
p { text-decoration: underline; }
```

### 4. `text-transform`
Transforma mayúsculas y minúsculas:

```css
h1 { text-transform: uppercase; }
p  { text-transform: capitalize; }
```

### 5. `letter-spacing` y `word-spacing`
Ajustan el espacio entre letras y palabras:

```css
h1 { letter-spacing: 2px; }
p  { word-spacing: 5px; }
```

### 6. `line-height`
Controla la altura de línea (interlineado):

```css
p { line-height: 1.6; }
```

---

## ✅ Buenas prácticas

- Usar **máximo 2 o 3 tipografías** distintas en un proyecto.  
- Preferir **unidades relativas** (`em`, `rem`) para tamaños.  
- Asegurar un **contraste suficiente** entre texto y fondo.  
- Evitar abusar de estilos como `italic` o `underline`, pues afectan a la legibilidad.  
- Usar **Google Fonts** u otros repositorios para mayor compatibilidad.  

---

## 📌 Resumen

- `font-family`, `font-size`, `font-weight`, `font-style` y `font-variant` controlan el estilo de la fuente.  
- Propiedades de texto: `color`, `text-align`, `text-decoration`, `text-transform`, `letter-spacing`, `word-spacing`, `line-height`.  
- La tipografía adecuada mejora la **usabilidad** y la **estética** de una web.  
