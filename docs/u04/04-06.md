# 4.6 Diseño responsivo con media queries

El **diseño responsivo** permite que una página web se **adapte automáticamente** al tamaño de pantalla del dispositivo (móvil, tablet, ordenador, TV).  
En CSS, la herramienta principal para conseguirlo son las **media queries**.

---

## 📱 ¿Qué es una media query?

Una **media query** aplica reglas CSS **solo si** se cumple una condición sobre el **medio** (media) o las características del dispositivo.

Sintaxis básica:
```css
@media (condición) {
  /* reglas CSS */
}
```

Ejemplo:
```css
@media (max-width: 600px) {
  body {
    background-color: lightblue;
  }
}
```

En este caso, el fondo será azul solo si el ancho de la ventana es **600px o menor**.

---

## 📏 Condiciones comunes

- `max-width`: aplicar estilos si el ancho es **menor o igual** al valor.  
- `min-width`: aplicar estilos si el ancho es **mayor o igual** al valor.  
- `max-height`, `min-height`: condiciones por altura.  
- `orientation: portrait|landscape`: según orientación del dispositivo.  
- `prefers-color-scheme: dark|light`: según tema oscuro/claro del usuario.  

Ejemplo:
```css
@media (min-width: 768px) and (max-width: 1024px) {
  /* Estilos solo para tablets */
}
```

---

## 🖥️ Mobile First vs Desktop First

- **Mobile First**: se diseñan primero estilos para pantallas pequeñas y se amplían con `min-width`.  
- **Desktop First**: se diseñan primero para escritorio y se reducen con `max-width`.  

Ejemplo (Mobile First):
```css
/* Base: móviles */
.container { font-size: 14px; }

/* Pantallas medianas */
@media (min-width: 768px) {
  .container { font-size: 16px; }
}

/* Pantallas grandes */
@media (min-width: 1200px) {
  .container { font-size: 18px; }
}
```

---

## 🧩 Ejemplo práctico: Grid responsivo

```css
.cards {
  display: grid;
  grid-template-columns: 1fr;
  gap: 1rem;
}

/* Tablets */
@media (min-width: 600px) {
  .cards {
    grid-template-columns: repeat(2, 1fr);
  }
}

/* Escritorio */
@media (min-width: 900px) {
  .cards {
    grid-template-columns: repeat(3, 1fr);
  }
}
```

Con esto, el número de columnas aumenta según el ancho de la pantalla.

---

## ✅ Buenas prácticas

- Usar **Mobile First**: más simple y eficiente en rendimiento.  
- Definir **puntos de ruptura (breakpoints)** lógicos: 600px (móviles), 768px (tablets), 1024px (laptops), 1200px+ (desktops grandes).  
- No usar demasiados breakpoints → mantener un diseño coherente.  
- Probar en varios navegadores y dispositivos.  
- Aprovechar media queries modernas (`prefers-color-scheme`, `prefers-reduced-motion`).  

---

## 📌 Resumen

- Las **media queries** permiten aplicar CSS según el tamaño de pantalla u otras características.  
- Se pueden usar con `min-width`, `max-width`, orientación, tema oscuro, etc.  
- En diseño responsivo se recomienda un enfoque **Mobile First**.  
- El objetivo es que la web sea usable y agradable en **todos los dispositivos**.
